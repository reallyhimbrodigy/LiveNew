{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch LiveGood",
      "program": "${workspaceFolder}/index.js",
      "cwd": "${workspaceFolder}"
    }
  ]
}
